<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.vibrator"
        android:required="false" />

    <queries>
        <package android:name="org.lsposed.manager" />
        <package android:name="io.github.lsposed.manager" />
    </queries>

    <application
        android:name=".HapticksApp"
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hapticks"
        tools:targetApi="33">

        <!-- LSPosed module metadata. Consumed by LSPosed Manager when the APK is installed on
             a rooted device that hosts LSPosed; ignored on every other device.

             xposedminversion=93 is the universal baseline LSPosed reports for the classic
             `de.robv.android.xposed.*` API (still the canonical entry point in LSPosed 1.10 /
             2026 builds — the newer libxposed interface runs alongside it, but the classic
             API is what our EdgeScrollHooks uses).

             xposedsharedprefs=true opts into LSPosed's in-kernel MODE_WORLD_READABLE shim,
             so XSharedPreferences reads in hooked target processes don't silently fall back
             to defaults when the module's prefs file was written on a recent Android. -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Hapticks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.HapticsAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!--
            Receives the broadcast fallback from EdgeScrollHooks when the hooked process
            lacks VIBRATE permission. Exported so LSPosed-injected code running under the
            target app's UID can reach it; the intent is explicit (ComponentName-targeted)
            and the receiver only fires a cached VibrationEffect, so spoofing a broadcast
            would cost the attacker nothing but their own vibration.
        -->
        <receiver
            android:name=".edge.EdgeHapticReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="com.hapticks.app.edge.ACTION_EDGE_HAPTIC" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
